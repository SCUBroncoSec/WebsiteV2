import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    output: 'export', // view production files via a server: npx serve out -l 3000
};

export default nextConfig;
